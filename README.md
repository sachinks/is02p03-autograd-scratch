# is02p03-autograd-scratch
"Every neural network is a directed acyclic graph of mathematical operations. A forward pass traverses the graph computing outputs from inputs. A backward pass traverses in reverse computing gradients via chain rule. requires_grad=True tells PyTorch to record each operation for later differentiation.
